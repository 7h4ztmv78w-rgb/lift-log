{
  "name": "lift-log",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first workout logging app for iPhone via Capacitor.",
  "scripts": {
    "build": "node scripts/build-web.js",
    "serve": "node server.js",
    "cap:sync": "npm run build && npx cap sync",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.4",
    "@capacitor/ios": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
